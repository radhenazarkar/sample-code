(function (FlightSearchEngine) {
  FlightSearchEngine.ENUMS = {
    TRIP_TYPE: {
      ONE_WAY: "one_way",
      RETURN_TRIP: "return_trip"
    },

    EVENTS: {
      MODEL: {
        SEARCH_START: "search_start",
        SEARCH_COMPLETE: "search_complete",
        FILTER_SEARCH: "filter_search"
      }
    }
  }
})(FlightSearchEngine);
